const sum = require("./sum");
const sub = require("./sum");

// test("adds 1 + 2 to equal 3", () => {
//   expect(sum(1, 2)).toBe(3);
// });

test("sub 1-2 t equal to -1", () => {
  expect(sub(1, 2)).toBe(-1);
});
